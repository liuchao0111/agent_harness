---
name: cron-task-380589
description: 存在一个循环定时任务，每分钟打印当前日期时间
type:project
---

定时任务 ID：`cron_380589`
- **Cron 表达式**：`* * * * *`（每分钟触发一次）
- **类型**：循环执行，持久化到磁盘
- **执行内容**：`echo %date% %time%`（实际环境退化为 `date` 命令）
- **状态**：已创建并正在运行
